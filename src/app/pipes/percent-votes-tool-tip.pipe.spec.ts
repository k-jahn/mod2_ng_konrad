import { PercentVotesToolTipPipe } from './percent-votes-tool-tip.pipe';

describe('PercentVotesToolTipPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentVotesToolTipPipe();
    expect(pipe).toBeTruthy();
  });
});
