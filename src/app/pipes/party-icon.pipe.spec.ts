import { PartyIconPipe } from './party-icon.pipe';

describe('PartyIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PartyIconPipe();
    expect(pipe).toBeTruthy();
  });
});
